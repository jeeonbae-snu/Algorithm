# 정리 작업 변경 요약

작업 일자: 2026-05-11 (2차 정리 반영)

## 변경 내역

### 1. 최상위 README.md 재작성
기존 BaekjoonHub 기본 안내문(2줄) → 다음 내용으로 교체:
- 풀이 현황 (사이트별 / 백준 난이도별 / 프로그래머스 레벨별)
- 주요 알고리즘 분포
- 폴더 구조 설명
- 사용 언어 통계

### 2. Dead Code 정리 — 36개 파일
주석 처리된 미사용 코드 블록을 제거했습니다.
- 1차 정리: 연속 2줄 이상의 dead code 제거 (21개 파일)
- 2차 정리: 빈 `#` 잔재 및 중첩 주석 제거 (15+3개 파일)

대표적인 정리 예시:
- `프로그래머스/3/42898. 등굣길` — 16줄 dead code 제거
- `프로그래머스/2/42746. 가장 큰 수` — 11줄 dead code 제거
- `백준/Gold/10942. 팰린드롬？` — 11줄 dead code 제거

### 3. 헤더 주석 추가 — 107개 파일
주석이 없던 코드 15줄+ 파일들 상단에 헤더 추가:

```python
# [Gold IV] 최단경로 (BOJ 1753)
# 분류: 그래프 이론, 최단 경로, 데이크스트라
# 접근: 우선순위 큐(heapq)로 시작점에서 각 노드까지의 최단거리 갱신
```

분류 정보는 README의 BOJ 분류를 우선 사용하며, 없을 경우 코드에서 휴리스틱으로 추출한 알고리즘을 사용합니다.

### 4. 잘못된 헤더 수정 — 11개 파일 (2차 정리, 2026-05-11 추가)

1차 헤더 자동 추가 시 "접근:" 줄에 BFS/DFS 보일러플레이트(`# 접근: 큐/스택으로 그래프(또는 격자)를 순회하며 조건 검사`)가 잘못 들어간 11개 파일의 분류/접근 줄을 실제 알고리즘에 맞게 재작성:

| 파일 | 수정 사항 |
|---|---|
| `백준/Gold/10942. 팰린드롬？` | 접근 → 2차원 DP 팰린드롬 체크 |
| `백준/Gold/15685. 드래곤 커브` | 접근 → 방향열 확장 시뮬레이션 |
| `백준/Gold/17299. 오등큰수` | 접근 → 빈도 사전 + 단조 스택 |
| `백준/Silver/12789. 도키도키 간식드리미` | 접근 → 보조 스택 시뮬레이션 |
| `백준/Silver/28278. 스택 2` | 접근 → 리스트 기반 스택 명령 처리 |
| `백준/Silver/4949. 균형잡힌 세상` | 접근 → 스택으로 (), [] 짝맞춤 |
| `백준/Silver/9012. 괄호` | 접근 → 스택으로 () 짝맞춤 |
| `프로그래머스/2/12909. 올바른 괄호` | 분류 BFS/DFS → 스택, 큐 / 접근 재작성 |
| `프로그래머스/2/42626. 더 맵게` | 분류 BFS/DFS, 다익스트라 → 힙(우선순위 큐) / 접근 재작성 |
| `프로그래머스/3/42628. 이중우선순위큐` | 분류 BFS/DFS → 힙(우선순위 큐) / 접근 재작성 |
| `프로그래머스/5/49190. 방의 개수` | 분류 BFS/DFS → 그래프 / 접근 오일러 공식 |

## 검증

- ✅ 240개 .py 파일 모두 `python3 -m py_compile` 통과 (문법 오류 없음)
- ✅ "접근:" 줄과 실제 코드 알고리즘 불일치 0건 (28개 파일은 BFS/DFS 보일러플레이트가 정상 사용 중)

## 미수정 항목 (선택)

다음은 수정하지 않고 그대로 두었습니다:
- **Windows 전각문자 치환 폴더명 8개** — `？ ！ －` 등은 Windows 파일시스템 제약 때문에 BaekjoonHub가 자동으로 생성한 것이므로 의도된 동작.
- **.java / .cc 파일** — `#` 주석 자동 추가 대상에서 제외 (각각 `//` 주석 사용).
- **헤더 누락 15줄+ 파일 ~87개** — 1차 정리 범위 외. 후속 작업으로 분리.
- **dead-code 잔재 ~31개 파일** — 단편적인 `# graph[a].append(...)` 등은 의도된 메모로 판단해 보존.
